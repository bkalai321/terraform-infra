aws_region="${aws_region}"
vpc_id="${vpc_id}"
name="${name}"
component="${component}"
image_id="${image_id}"
availability_zone="${availability_zone}"
subnet_id="${subnet_id}"
instance_type="${instance_type}"
myVersion="${myVersion}"
s3_terraform_bucket="${s3_terraform_bucket}"
user_data="${user_data}"
jenkinsUser="${jenkinsUser}"
