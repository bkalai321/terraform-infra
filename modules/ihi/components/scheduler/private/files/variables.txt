name="${name}"
component="${component}"
image_id="${image_id}"
availability_zone="${availability_zone}"
instance_type="${instance_type}"
jenkinsUser="${jenkinsUser}"
build_url="${build_url}"
folderPath="${folderPath}"
terminateInHrs="${terminateInHrs}"